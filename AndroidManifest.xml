<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.aces.townportal"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION" />
    <uses-permission android:name="android.permission.CONTROL_LOCATION_UPDATES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_pc"
        android:label="@string/app_name"
        android:theme="@style/customTheme" >
        <activity
            android:name="com.android.aces.townportal.SplashActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.android.aces.townportal.MainActivity"
            android:label="@string/app_name" >

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
            </activity>
        <activity
            android:name="com.android.aces.townportal.MapActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.android.aces.townportal.GooglePlacesMap"
            android:label="Google Places Map"
            android:theme="@android:style/Theme.NoTitleBar" >

            <!--
            <intent-filter>
                <action android:name="android.intent.action.GooglePlacesMap" />
                <category android:name="android.intent.category.GooglePlacesMap" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name="com.android.aces.townportal.PlaceDetailActivity"
            android:label="@string/title_activity_place_detail" >
        </activity>
    </application>

</manifest>
